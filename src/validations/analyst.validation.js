// const Joi = require('joi');

const getRevenue = {
  // query: Joi.object()
  //   .keys({
  //     period: Joi.string(),
  //   })
  //   .unknown(true),
};

const getSaleProductToday = {};

module.exports = {
  getRevenue,
  getSaleProductToday,
};
